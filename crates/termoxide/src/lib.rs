//! The TermOxide framework crate.
//!
//! This crate is the entry point applications depend on: it re-exports the
//! layers below it and, from the next commit onward, owns the main loop that
//! drives them. It is a library only — the binary that used to live here was
//! never part of the published surface.
